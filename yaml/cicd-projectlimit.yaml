apiVersion: "v1"
kind: "LimitRange"
metadata:
  name: "cicd-core-resource-limits" 
spec:
  limits:
    - type: "Pod"
      max:
        cpu: "4000m" 
        memory: "700Ti" 
      min:
        cpu: "1000m" 
        memory: "700Ti" 
    - type: "Container"
      max:
        cpu: "1500m" 
        memory: "700Ti" 
      min:
        cpu: "500m" 
        memory: "700Ti" 
      default:
        cpu: "500m" 
        memory: "700Ti" 
      defaultRequest:
        cpu: "500m"
        memory: "700Ti" 
      maxLimitRequestRatio:
        cpu: "1" 
