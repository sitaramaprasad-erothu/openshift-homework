apiVersion: "v1"
kind: "LimitRange"
metadata:
  name: "env-core-resource-limits" 
spec:
  limits:
    - type: "Pod"
      max:
        cpu: "700m" 
        memory: "700Mi" 
      min:
        cpu: "500m" 
        memory: "500Mi" 
    - type: "Container"
      max:
        cpu: "400m" 
        memory: "400Mi" 
      min:
        cpu: "300m" 
        memory: "300Mi" 
      default:
        cpu: "300m" 
        memory: "300Mi" 
      defaultRequest:
        cpu: "300m"
        memory: "300Mi" 
      maxLimitRequestRatio:
        cpu: "2" 
